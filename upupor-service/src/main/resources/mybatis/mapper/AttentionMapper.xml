<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upupor.service.dao.mapper.AttentionMapper">
  <resultMap id="BaseResultMap" type="com.upupor.service.dao.entity.Attention">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="attention_id" jdbcType="VARCHAR" property="attentionId" />
    <result column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="attention_user_id" jdbcType="VARCHAR" property="attentionUserId" />
    <result column="attention_status" jdbcType="INTEGER" property="attentionStatus" />
    <result column="create_time" jdbcType="BIGINT" property="createTime" />
    <result column="sys_update_time" jdbcType="TIMESTAMP" property="sysUpdateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    id, attention_id, user_id, attention_user_id, attention_status, create_time, sys_update_time
  </sql>


  <select id="checkExists" resultType="java.lang.Integer">
    select count(*) from attention where attention_status = 0
    and user_id = #{userId,jdbcType=VARCHAR}
    and attention_user_id = #{attentionUserId,jdbcType=VARCHAR}
  </select>

  <!--获取我的关注数-->
  <select id="getAttentionNum" resultType="java.lang.Integer">
    select count(*) from attention where attention_status = 0
    and user_id = #{userId,jdbcType=VARCHAR}
  </select>

  <!--获取我的关注者-->
  <select id="getAttentions" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from attention where attention_status = 0
    and user_id = #{userId,jdbcType=VARCHAR}
    order by create_time desc
  </select>

</mapper>